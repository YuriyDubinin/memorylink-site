import React, {useEffect} from 'react';

import './MiddleBlock.scss';

const MiddleBlock = ({children}) => {
    return <div className="middle-block">{children}</div>;
};

export default MiddleBlock;
